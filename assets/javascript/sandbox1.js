let mountains = [["Sugarloaf", 40], ["King Pine", 20], ["Sunday River", 35],
                ["Sunapee", 28]];
let largestPeak = (mountains = [0][1]) => {
  if(mountains.every(n => n === 0)) {
    return '0';
  }
  mountains.sort((a, b))
}